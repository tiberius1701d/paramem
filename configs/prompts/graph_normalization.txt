Below are extracted personal facts, one per line as: subject | predicate | object.

Collapse DUPLICATES. Two facts are duplicates when they have the SAME subject and the SAME object but different predicates that express the same relationship — merge them into ONE relation using the clearest predicate already present. Keep every distinct fact unchanged. Do not invent facts; never output a subject, predicate, or object that is not already present in the input.

In the examples below every JSON literal uses double braces {{ }} so the format engine collapses them to single braces. The output you emit must use plain single-brace JSON.

POSITIVE example — three different synonym pairs all merge; distinct facts kept:
Facts:
Alex | works_for | Acme
Alex | employed_by | Acme
Alex | resides_in | Portland
Alex | lives_in | Portland
Alex | authored | The Paper
Alex | wrote | The Paper
Alex | likes | hiking
Output:
{{"relations": [{{"subject": "Alex", "predicate": "works_for", "object": "Acme"}}, {{"subject": "Alex", "predicate": "lives_in", "object": "Portland"}}, {{"subject": "Alex", "predicate": "authored", "object": "The Paper"}}, {{"subject": "Alex", "predicate": "likes", "object": "hiking"}}]}}

NEGATIVE example — same subject but DIFFERENT objects are NOT duplicates; keep both:
Facts:
Sam | lives_in | Berlin
Sam | lives_in | Munich
Output:
{{"relations": [{{"subject": "Sam", "predicate": "lives_in", "object": "Berlin"}}, {{"subject": "Sam", "predicate": "lives_in", "object": "Munich"}}]}}

NEGATIVE example — asymmetric / distinct predicates on the same pair are NOT duplicates (manages/reports_to, parent_of/child_of, taught/studied_under); keep both:
Facts:
River | manages | Casey
Casey | reports_to | River
Output:
{{"relations": [{{"subject": "River", "predicate": "manages", "object": "Casey"}}, {{"subject": "Casey", "predicate": "reports_to", "object": "River"}}]}}

Facts:
{fact_lines}

Output the deduplicated facts as JSON: {{"relations": [{{"subject": "...", "predicate": "...", "object": "..."}}]}}
