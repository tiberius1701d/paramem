You are operating over a pre-merged, cross-transcript knowledge graph. The triples below were extracted from multiple conversations and merged into one cumulative graph. Emit two outputs: (1) second-order RELATIONS implied by the input but not present in it, and (2) SAME_AS pairs for nodes that refer to the same real-world entity under different surface forms.

Required for both outputs:
- `subject` / `object` / `canonical` / `variant` must be exact node names from the input graph.
- `relation_type` ∈ {{factual, temporal, preference, social}}; use `social` for interpersonal relations, `factual` otherwise.
- Confidence ≥ 0.7. Emit nothing rather than guess.
- each input triple includes a `speaker_id`; a non-empty `speaker_id` marks the subject as a speaker.
- each emitted relation must include `"symmetric": true` or `"symmetric": false`.


## Part 1 — RELATIONS

Symmetric predicates hold in both directions (colleague_of, friend_of, married_to, sibling_of, classmate_of, teammate_of, neighbor_of, knows, family_of, shares_interest_with, attended_with, ...) and must carry `"symmetric": true`. Directional predicates (mentored_by, manages, parent_of, works_at) carry `"symmetric": false`. For non-speaker symmetric pairs emit ONE direction only (the system canonicalizes direction). When the input triple's `speaker_id` is absent or empty, the speaker↔speaker exception below does not fire — emit a single direction (safe default).

EXCEPTION — when BOTH endpoints are speakers (each input triple carries a non-empty `speaker_id`), emit BOTH directions of a symmetric relation `(A,P,B)` AND `(B,P,A)`, each with `"symmetric": true`. For speaker↔non-speaker or non-speaker↔non-speaker symmetric relations, emit ONE direction.

Emit cross-session relations that are implied by shared context but not already in the input.

POSITIVE — input has `works_at(Alice, Acme)[speaker_id=Alice]` AND `works_at(Bob, Acme)[speaker_id=Bob]`:
→ EMIT `colleague_of(Alice, Bob)` with `"symmetric": true` AND `colleague_of(Bob, Alice)` with `"symmetric": true` — speaker↔speaker exception: both directions.

POSITIVE — input has `works_at(Alice, Acme)[speaker_id=]` AND `works_at(Bob, Acme)[speaker_id=]` (no speaker_id):
→ EMIT `colleague_of(Alice, Bob)` with `"symmetric": true`, single direction only — speaker exception does not fire.

POSITIVE — input has `attended(Alice, Stanford)`, `attended(Bob, Stanford)`, AND `study_partner_of(Alice, Bob)`:
→ EMIT `classmate_of(Alice, Bob)` — supported by both shared institution AND an explicit interaction triple.

NEGATIVE — input has only `lives_in(Alice, Portland)` AND `lives_in(Bob, Portland)`:
→ EMIT NOTHING. City co-presence alone is not evidence of `neighbor_of` — would need same street or building.

NEGATIVE — input has only `attended(Alice, Stanford)` AND `attended(Bob, Stanford)`:
→ DO NOT emit `knows(Alice, Bob)`. Graph co-presence alone is not evidence of acquaintance. Prefer a specific predicate (colleague_of, classmate_of, friend_of, family_of, neighbor_of); if none is justified, emit nothing.

NEGATIVE — input already contains `colleague_of(Alice, Bob)` and NEITHER is a speaker:
→ DO NOT re-emit `colleague_of(Bob, Alice)` — symmetric pair, single direction only. DO NOT re-emit any input triple.

NEGATIVE — input has triples about Alice's program work but no node named `"€200M+"`, `"12 months"`, or `"Independent"`:
→ DO NOT emit `(Alice, managed_program_value_of, "€200M+")`, `(Alice, recovered_schedule_delay_of, "12 months")`, or `(Alice, currently_employed_at, "Independent")`. Literal values (amounts, durations, dates, percentages, bare adjectives) are NOT graph nodes. The `subject` and `object` of every emitted relation must be a string that already appears as a node in the input graph — verbatim. If no existing node captures the fact, emit nothing rather than minting a new one. Session-level extraction handles literal values as attributes; this pass does not.


## Part 2 — SAME_AS pairs

Emit `[canonical, variant]` pairs where canonical is the more complete or specific form. Strip honorifics (Mr., Mrs., Ms., Dr., Prof., Professor) when judging identity. Emit each logical pair exactly once.

POSITIVE — graph has `Mr. Yang` and `Yang Ming`:
→ `["Yang Ming", "Mr. Yang"]` — full name canonical; honorific stripped before comparison.

POSITIVE — graph has `Robert Smith` and `Bob Smith` (same surname, formal/informal of given name):
→ `["Robert Smith", "Bob Smith"]` — formal canonical, informal variant.

POSITIVE — graph has `Alice` and `Alicia Smith` AND both share role / employer attributes in the input:
→ `["Alicia Smith", "Alice"]` — full name canonical; nickname → full name. Evidence comes from shared attributes, not from the names alone.

NEGATIVE — graph has `Zhang Min` and `Wang Min`:
→ DO NOT MERGE. Different surname tokens → different people, even when the given-name token matches.

NEGATIVE — graph has `Sara` and `Sarah` with no shared attributes or relations:
→ DO NOT MERGE. Sounds similar but no evidence of shared identity. Phonetic similarity alone is insufficient.

NEGATIVE — pair `[Yang Ming, Mr. Yang]` already emitted earlier in this same response:
→ DO NOT also emit `[Mr. Yang, Yang Ming]` or any minor surface variant. One logical pair, one entry.


## Input triples

{triples_json}


## Output

Return ONLY a JSON object:
```
{{
  "relations": [
    {{"subject": "...", "predicate": "...", "object": "...", "relation_type": "...", "confidence": 0.0, "symmetric": false}}
  ],
  "same_as": [
    ["canonical_name", "variant_name"]
  ]
}}
```

Both arrays may be empty. No other keys, no commentary outside the JSON block.
