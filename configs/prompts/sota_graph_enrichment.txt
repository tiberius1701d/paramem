You are operating over a pre-merged, cross-transcript knowledge graph. The triples below were extracted from multiple conversations and merged into one cumulative graph. Emit two outputs: (1) second-order RELATIONS implied by the input but not present in it, and (2) SAME_AS pairs for nodes that refer to the same real-world entity under different surface forms.

Note: nodes in scope for the operator's privacy policy appear below as system-generated placeholder tokens (e.g. `Person_1`, `Org_1`); nodes outside that scope, and every speaker endpoint (`speaker0`, `speaker1`, etc.), appear in their real surface form — do not treat every `subject`/`object` as a token. Do not attempt to de-anonymize, rename, or guess the identity behind a placeholder token, and do not invent a new token for an entity not already present in the input. Do NOT propose a `same_as` pair between two `speaker{{N}}` identifiers — they are system identifiers, not human names, and can never be the same person under a different name.

Required for both outputs:
- `subject` / `object` / `canonical` / `variant` must be exact node names from the input graph.
- `relation_type` ∈ {{factual, temporal, preference, social}}; use `social` for interpersonal relations, `factual` otherwise.
- Confidence ≥ 0.7. Emit nothing rather than guess.
- each input triple includes a `speaker_id`; a non-empty `speaker_id` marks the subject as a speaker.
- each emitted relation must include `"symmetric": true` or `"symmetric": false`.


## Part 1 — RELATIONS

Symmetric predicates hold in both directions (colleague_of, friend_of, married_to, sibling_of, classmate_of, teammate_of, neighbor_of, knows, family_of, shares_interest_with, attended_with, ...) and must carry `"symmetric": true`. Directional predicates (mentored_by, manages, parent_of, works_at) carry `"symmetric": false`. For non-speaker symmetric pairs emit ONE direction only (the system canonicalizes direction). When the input triple's `speaker_id` is absent or empty, the speaker↔speaker exception below does not fire — emit a single direction (safe default).

EXCEPTION — when BOTH endpoints are speakers (each input triple carries a non-empty `speaker_id`), emit BOTH directions of a symmetric relation `(A,P,B)` AND `(B,P,A)`, each with `"symmetric": true`. For speaker↔non-speaker or non-speaker↔non-speaker symmetric relations, emit ONE direction.

Emit cross-session relations that are implied by shared context but not already in the input.

POSITIVE — input has `works_at(Person_1, Org_1)[speaker_id=Person_1]` AND `works_at(Person_2, Org_1)[speaker_id=Person_2]`:
→ EMIT `colleague_of(Person_1, Person_2)` with `"symmetric": true` AND `colleague_of(Person_2, Person_1)` with `"symmetric": true` — speaker↔speaker exception: both directions.

POSITIVE — input has `works_at(Person_1, Org_1)[speaker_id=]` AND `works_at(Person_2, Org_1)[speaker_id=]` (no speaker_id):
→ EMIT `colleague_of(Person_1, Person_2)` with `"symmetric": true`, single direction only — speaker exception does not fire.

POSITIVE — input has `attended(Person_1, Org_2)`, `attended(Person_2, Org_2)`, AND `study_partner_of(Person_1, Person_2)`:
→ EMIT `classmate_of(Person_1, Person_2)` — supported by both shared institution AND an explicit interaction triple.

NEGATIVE — input has only `lives_in(Person_1, City_1)` AND `lives_in(Person_2, City_1)`:
→ EMIT NOTHING. City co-presence alone is not evidence of `neighbor_of` — would need same street or building.

NEGATIVE — input has only `attended(Person_1, Org_2)` AND `attended(Person_2, Org_2)`:
→ DO NOT emit `knows(Person_1, Person_2)`. Graph co-presence alone is not evidence of acquaintance. Prefer a specific predicate (colleague_of, classmate_of, friend_of, family_of, neighbor_of); if none is justified, emit nothing.

NEGATIVE — input already contains `colleague_of(Person_1, Person_2)` and NEITHER is a speaker:
→ DO NOT re-emit `colleague_of(Person_2, Person_1)` — symmetric pair, single direction only. DO NOT re-emit any input triple.

NEGATIVE — input has triples about Person_1's program work but no node named `"€200M+"`, `"12 months"`, or `"Independent"`:
→ DO NOT emit `(Person_1, managed_program_value_of, "€200M+")`, `(Person_1, recovered_schedule_delay_of, "12 months")`, or `(Person_1, currently_employed_at, "Independent")`. Literal values (amounts, durations, dates, percentages, bare adjectives) are NOT graph nodes. The `subject` and `object` of every emitted relation must be a string that already appears as a node in the input graph — verbatim (the token, e.g. `Person_1`, not the real name it stands for). If no existing node captures the fact, emit nothing rather than minting a new one. Session-level extraction handles literal values as attributes; this pass does not.


## Part 2 — SAME_AS pairs

Under the default `{{"person"}}` cloud-egress scope, person nodes are opaque tokens by the time this pass runs, so a SAME_AS judgment over two surfaces of the same PERSON is not possible here — do not attempt it (see the note above). The surfaces this pass can actually judge are organizations, places, and other non-person things, which reach it verbatim. Emit `[canonical, variant]` pairs where canonical is the more complete or specific form. Strip legal-entity suffixes (Inc., Corp., Corporation, LLC, Ltd., Co.) and expand common abbreviations when judging identity. Emit each logical pair exactly once.

POSITIVE — graph has `Acme Corp` and `Acme Corporation`:
→ `["Acme Corporation", "Acme Corp"]` — full legal name canonical; suffix abbreviation stripped before comparison.

POSITIVE — graph has `IBM` and `International Business Machines`:
→ `["International Business Machines", "IBM"]` — full name canonical, initialism variant.

POSITIVE — graph has `NYC` and `New York City` AND both share attributes/relations tying them to the same location context in the input:
→ `["New York City", "NYC"]` — full name canonical; abbreviation → full name. Evidence comes from shared attributes, not from the strings alone.

NEGATIVE — graph has `Bank of America` and `Bank of the West`:
→ DO NOT MERGE. Shared "Bank of" prefix, distinct institutions — a shared token is not evidence of identity.

NEGATIVE — graph has `Aetna` and `Etna` with no shared attributes or relations:
→ DO NOT MERGE. Sounds similar but no evidence of shared identity. Phonetic similarity alone is insufficient.

NEGATIVE — pair `[International Business Machines, IBM]` already emitted earlier in this same response:
→ DO NOT also emit `[IBM, International Business Machines]` or any minor surface variant. One logical pair, one entry.


## Input triples

{triples_json}


## Output

Return ONLY a JSON object:
```
{{
  "relations": [
    {{"subject": "...", "predicate": "...", "object": "...", "relation_type": "...", "confidence": 0.0, "symmetric": false}}
  ],
  "same_as": [
    ["canonical_name", "variant_name"]
  ]
}}
```

Both arrays may be empty. No other keys, no commentary outside the JSON block.
