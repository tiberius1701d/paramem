You are reviewing extracted personal facts from a voice assistant conversation. The facts and transcript have been anonymized — all names are placeholders (Person_1, City_1, etc.).

Your task is threefold:

## 1. Coreference Resolution
Resolve pronouns and implicit references by examining the conversation context:
- "my wife" → add married_to(Person_1, Person_2) if the wife's placeholder is identifiable from context
- "we went there last summer" → add visited(Person_1, Location_1, "last summer") with subjects resolved
- "I" / "me" / "my" always refers to the speaker (Person_1 unless context indicates otherwise)
- "our kids" → add has_children(Person_1, ...) if not already captured
- Possessive relationships: "my sister's husband" → chain the relations

## 2. Compound Fact Splitting
Split multi-fact statements into individual relations:
- "grew up in City_1 and studied in City_2" → grew_up_in(Person_1, City_1) + studied_in(Person_1, City_2)
- "likes hiking and cooking" → likes(Person_1, hiking) + likes(Person_1, cooking)

## 3. Noise Removal
REMOVE facts that are:
- Inferred from casual questions (asking about weather in a city ≠ living there)
- Device/system identifiers (media_player.sonos_office)
- Implausible entity names (likely speech recognition errors)
- Descriptions of conversation events, not personal facts

For each fact: KEEP, MODIFY (resolve coreference/split), or REMOVE.
You may ADD new facts discovered through coreference resolution that the extractor missed.

Conversation transcript (anonymized):
{transcript}

Extracted facts (anonymized):
{facts_json}

Return ONLY a JSON array of the final enriched facts. Each fact must have: subject, predicate, object, relation_type, confidence.
Set confidence to 0.9 for coreference-resolved facts, 0.8 for inferred facts. Keep original confidence for unchanged facts.
If no facts survive, return [].
