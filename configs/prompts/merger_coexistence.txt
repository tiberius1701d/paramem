Classify the cardinality of the predicate "{predicate}".

MULTI-VALUED → COEXIST (one subject can hold or do this with many different objects over time — creating, building, writing, owning, or knowing many things):
- has_pet → COEXIST
- developed → COEXIST
- created → COEXIST
- built → COEXIST
- wrote → COEXIST
- designed → COEXIST
- speaks → COEXIST
- visited → COEXIST
- owns → COEXIST
- worked_on → COEXIST
- hobby → COEXIST

SINGLE-VALUED → REPLACE (a new value supersedes the old — only one can be current at a time):
- date_of_birth → REPLACE
- primary_residence → REPLACE
- current_employer → REPLACE
- lives_in → REPLACE
- works_at → REPLACE
- married_to → REPLACE

Predicate: {predicate}

Reply with EXACTLY one of:
- COEXIST
- REPLACE
