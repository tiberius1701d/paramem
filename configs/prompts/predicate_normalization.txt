You are given a list of predicates that all describe the same (subject, object) pair in a knowledge graph. Your task is to cluster these predicates into synonym groups — predicates that express the SAME real-world relationship with the same direction.

RULES:
1. Cluster only predicates that are genuine synonyms (same real-world relationship, same direction).
2. NEVER merge predicates that are asymmetric or directional (e.g. "manages" vs "reports_to", "parent_of" vs "child_of", "taught" vs "studied_under").
3. Each cluster may have 1 or more predicates. A single-element cluster means that predicate has no synonym in this set — keep it as-is.
4. Do not invent predicates. Only use predicates from the input list.
5. Output valid JSON only. No prose, no markdown fences.

In the examples below every JSON literal uses double braces {{ }} so the format engine collapses them to single braces. The output you emit must use plain single-brace JSON.

POSITIVE example — two synonym pairs cluster together; the distinct predicate is its own cluster:
Predicates: ["works_for", "employed_by", "resides_in"]
Output:
{{"clusters": [["works_for", "employed_by"], ["resides_in"]]}}

POSITIVE example — three synonyms collapse into one cluster:
Predicates: ["works_at", "works_for", "employed_at"]
Output:
{{"clusters": [["works_at", "works_for", "employed_at"]]}}

NEGATIVE example — asymmetric / directional predicates are NOT synonyms; keep in separate clusters:
Predicates: ["manages", "supervises", "reports_to"]
Output:
{{"clusters": [["manages", "supervises"], ["reports_to"]]}}

NEGATIVE example — entirely distinct predicates with no synonyms; each in its own cluster:
Predicates: ["born_in", "works_for", "lives_in"]
Output:
{{"clusters": [["born_in"], ["works_for"], ["lives_in"]]}}

Predicates: {predicates_json}

Output the synonym clusters as JSON: {{"clusters": [["predA", "predB"], ["predC"]]}}
